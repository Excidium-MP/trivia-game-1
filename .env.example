# Upstash Redis (via Vercel Marketplace → auto-added when you link a store).
# Leave these blank for LOCAL single-machine testing: the app falls back to an
# in-memory store that works across browser tabs on one dev server.
#
# For a real multi-device / deployed game, these MUST be set.
KV_REST_API_URL=
KV_REST_API_TOKEN=
